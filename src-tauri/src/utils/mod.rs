/// Utility helpers shared across the `src-tauri` crate.
///
/// These utilities are not tied to any single subsystem (API, config, etc.) and
/// are intended for broad reuse within the crate.
pub mod ipc_guards;
pub mod network_errors;
